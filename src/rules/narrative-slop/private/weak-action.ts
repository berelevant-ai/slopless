export const SUBORDINATING_MARKERS: ReadonlySet<string> = new Set(
  "after although as because before if once since though unless until when whenever where whereas while that which who whom whose whether what how whence whereof".split(
    " "
  )
);

export const SENSORIAL_COMPLEMENTS: ReadonlySet<string> = new Set(
  "cold dark heavy haze blur fog low quiet sharp still tight tired".split(" ")
);
