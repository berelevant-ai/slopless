# Relative signposting and evidence limitation coverage

## Goal

- Fixture3 verifies both requested constructions through public Slopless findings.

## Approach

- Specular tree and content checks verify private matcher placement, generalized slot constants, and existing public rule IDs.
- A typed plan-local verifier parses TypeScript imports and package exports to enforce private ownership, allowed shared dependencies, required owner imports, and the unchanged public surface.
- Fixture3 verifies supplied examples, generalized verb and complement variants, literal controls, scientific controls, standalone controls, detailed follow-ups, messages, and ranges.
- Repository validation verifies type checking, linting, formatting, spelling, and full type coverage.

## Key decisions

- Specular forbids separate public relative-signposting and evidence-limitation rules.
- Fixture3 verifies that only evidence-proxy subjects enter the adjacent-sentence template.
- An adversarial reviewer checks generalized coverage and module ownership after implementation.

## Files to modify

- Specular requires both existing public owners, both private matcher files, and both public case files.
- Fixture3 owns the approved syntactic-pattern output.
