# Textlint 15.8 update

<!-- textlint-disable slopless/coleman-liau, slopless/flesch-kincaid, slopless/gunning-fog -->

## Summary

Updated the Textlint runtime and both directly imported Textlint type packages
from 15.7.1 to 15.8.0 as one synchronized dependency change.

## Decisions made

- Kept `textlint`, `@textlint/types`, and `@textlint/ast-node-types` on the
  same version because Slopless imports the runtime's public types directly.
- Replaced three independent Dependabot pull requests with one change so
  `main` never contains mixed Textlint versions.
- Accepted no fixture changes. Textlint 15.8 preserves all approved Slopless
  output.

## Key files for context

- `.plans/2026-08-05-155908-textlint-15-8.md`
- `.plans/2026-08-05-155908-textlint-15-8.spec.json`
- `package.json`
- `pnpm-lock.yaml`

## Verification

- `pnpm run validate` passes.
- `specular lint` and `specular verify` pass.
- `fixture3 doctor` and `fixture3 check --all` pass.
- `pnpm audit` reports zero vulnerabilities.

## Next steps

- Merge the combined update after GitHub CI passes.
- Close Dependabot pull requests 109, 110, and 111 as superseded.
- Rebase release pull request 112 onto the updated `main` branch.

<!-- textlint-enable slopless/coleman-liau, slopless/flesch-kincaid, slopless/gunning-fog -->
