Summary:
Prepared the 0.2.23 release for the no-fragment pair detector updates. This release includes `No X. No Y.` fragment-stack detection and the follow-up correction that flags duplicate clipped no-fragment pairs.

Decisions made:
- Bumped `package.json` from 0.2.22 to 0.2.23 as a patch release.
- Released from a clean worktree based on `origin/development`, not from the dirty local development checkout, so unrelated term-policy edits do not enter the package.
- Kept the release path aligned with the repository workflow: merge to `main`, create a GitHub Release tag, and let GitHub Actions publish to npm with provenance.

Key files for context:
- `package.json`
- `src/rules/syntactic-patterns/repetition/private/fragment-stack-detector.ts`
- `behavior/fixtures/textlint-rules/cases/syntactic-patterns/hits.md`
- `behavior/golden/textlint-rules-cases-syntactic-patterns/approved.normalized.json`

Next steps:
- Run validation in the clean release worktree.
- Push the release branch, merge it into `main`, publish the GitHub Release, verify npm, and install the published package locally.
